<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Access - Barcode System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        body { background: #f8f9fa; }
        .scanner-frame { border: 3px dashed #28a745; border-radius: 15px; padding: 20px; background: white; }
        .result-box { border-radius: 15px; padding: 20px; margin: 10px 0; }
        .granted { background: linear-gradient(135deg, #d4edda, #c3e6cb); border-left: 5px solid #28a745; }
        .denied { background: linear-gradient(135deg, #f8d7da, #f5c6cb); border-left: 5px solid #dc3545; }
    </style>
</head>
<body>
    <div class="container py-4">
        <div class="text-center mb-4">
            <h1 class="display-6">
                <i class="fas fa-qrcode text-primary me-2"></i>
                Access Verification
            </h1>
            <p class="lead">Scan your barcode for access</p>
        </div>

        <div class="scanner-frame text-center">
            <i class="fas fa-camera fa-3x text-success mb-3"></i>
            <h4>Ready to Scan</h4>
            <p class="text-muted">Point your camera at the QR code</p>
            
            <div class="mt-3">
                <small class="text-muted">
                    <i class="fas fa-info-circle me-1"></i>
                    Your camera should automatically detect QR codes
                </small>
            </div>
        </div>

        <div class="text-center mt-4">
            <a href="/verify" class="btn btn-outline-primary">
                <i class="fas fa-keyboard me-2"></i>Enter Code Manually
            </a>
        </div>
    </div>

    <script>
        // If accessed with a code parameter, auto-submit for verification
        const urlParams = new URLSearchParams(window.location.search);
        const code = urlParams.get('code');
        
        if (code) {
            // Auto-submit the form for verification
            setTimeout(() => {
                window.location.href = `/verify?code=${encodeURIComponent(code)}`;
            }, 1000);
        }
    </script>
</body>
</html>